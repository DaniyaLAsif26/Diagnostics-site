import Button from '@mui/material/Button';

export default function NavSearch() {

    return (
        <div className="nav-signIn">
                <Button
                    variant="contained"
                    size="medium">
                    Sign in
                </Button>
            </div>
    )
}